<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".views.CustomerDetailsActivity">
<include
    android:id="@+id/top_bar"
    app:layout_constraintTop_toTopOf="parent"
    layout="@layout/custom_title_bar"
    >
</include>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        app:layout_constraintTop_toBottomOf="@id/top_bar"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_width="match_parent"
        android:layout_marginTop="15dp"
        android:layout_height="0dp"
        />
    <Button
        android:id="@+id/btn_give"
        android:layout_width="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        android:text="YOU GAVE Rs"
        android:backgroundTint="@color/red"
        android:layout_margin="@dimen/_15sdp"
        app:layout_constraintEnd_toStartOf="@id/btn_got"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_height="wrap_content"/>
    <Button
        android:id="@+id/btn_got"
        android:layout_width="0dp"
        android:text="YOU GOT Rs"
        android:backgroundTint="@color/green"
        android:layout_margin="@dimen/_15sdp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/btn_give"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_height="wrap_content"/>
</androidx.constraintlayout.widget.ConstraintLayout>